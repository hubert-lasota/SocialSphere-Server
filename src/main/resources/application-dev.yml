spring:
  application:
    name: SocialSphereBackend

  datasource:
    url: jdbc:sqlserver://localhost:1433;database=SocialSphereDev;trustServerCertificate=true;
    username: hubert
    password: root123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  liquibase:
    change-log: classpath:database/changelog-dev.xml

  jpa:
    properties:
      hibernate:
        format_sql=true:

    open-in-view: true

logging:
  level:
    org:
      hibernate:
        SQL: TRACE
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
    org.hl.socialspherebackend:
        application:
          user: DEBUG
    org.springframework:
      jdbc:
        core: TRACE
        datasource: TRACE
      transaction: TRACE
      hibernate: TRACE



jwt:
  secret: 951b507db97ad30ab856ce9e0751a0a22c3b4c83db0b1b75537cd93996fa4c58
  expiration_days: 14